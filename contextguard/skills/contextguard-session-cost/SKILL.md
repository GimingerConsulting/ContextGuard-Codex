---
name: contextguard-session-cost
description: Show ContextGuard session-level token and API-cost savings for the current Codex thread. Invoke explicitly with $contextguard-session-cost.
---

# ContextGuard Session Cost

Run:

```bash
"$PLUGIN_ROOT/scripts/contextguard" session-cost
```

Label token and API values as local estimates, not verified Codex server-side billing.