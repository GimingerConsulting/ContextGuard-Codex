---
name: contextguard-lifetime-savings
description: Show cumulative ContextGuard savings across all Codex sessions for this project. Invoke explicitly with $contextguard-lifetime-savings.
---

# ContextGuard Lifetime Savings

Run:

```bash
"$PLUGIN_ROOT/scripts/contextguard" lifetime-savings
```

Label token and API values as local estimates. Lifetime totals accumulate when sessions end or new ones start.